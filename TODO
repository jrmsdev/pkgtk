* package upgrade fetch only

* pkgrepo: remove existent config

* pkgrepo: create a new config (using /etc/pkg/FreeBSD.conf as template)

* (xdg/)desktop integration: icons, menu entry and such...
  maybe systray support too?

* do not allow closing windows if background proc is running

* go to list of installed packages after installing a package from a search?
